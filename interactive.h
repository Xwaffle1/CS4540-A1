#ifndef INTERACTIVE_H
#define INTERACTIVE_H
extern void doInteractive(zipTowns arrs, int length);
extern int searchZip(zipTowns arrs, char search[], int length);
extern int searchTown(zipTowns arrs, char search[], int length);


#endif // last line of file
